# Fundos da página de login

Coloque aqui:

- `bg-desktop.png` — arte horizontal em **tela cheia** no desktop (`background-position: left center` para priorizar a área livre da arte)
- `bg-mobile.png` — arte vertical em **tela cheia** no mobile

O app referencia `/login/bg-desktop.png` e `/login/bg-mobile.png` (com `?v=` no código para cache bust ao trocar os arquivos).
